fastapi>=0.109.0
uvicorn[standard]>=0.27.0
supabase>=2.0.0
apscheduler>=3.10.0
python-dotenv>=1.0.0
pydantic-settings>=2.0.0
PyJWT>=2.8.0

# Panchang computation — Swiss Ephemeris via drik-panchanga wrapper
# drik-panchanga wraps pyswisseph and is purpose-built for Hindu lunisolar calendar.
# It computes tithi at local sunrise using Lahiri ayanamsha (Government of India standard).
# Handles kshaya (skipped) and adhika (repeated) tithi edge cases.
pyswisseph>=2.10.3
# Install drik-panchanga from GitHub (not yet on PyPI stable):
# pip install git+https://github.com/webresh/drik-panchanga.git
# Or pin the wheel once available: drik-panchanga>=0.9.0

# Razorpay payment gateway (wire TODO blocks in payments.py once keys are set)
razorpay>=1.4.2

# Email notifications for vendor assignments
# Uses Python stdlib smtplib — no extra package needed for basic SMTP.
# For production-grade delivery, add sendgrid or AWS SES SDK here.
