fastapi>=0.109.0
uvicorn[standard]>=0.27.0
supabase>=2.0.0
apscheduler>=3.10.0
python-dotenv>=1.0.0
pydantic-settings>=2.0.0
PyJWT>=2.8.0

# Panchang computation — Swiss Ephemeris (gold standard, used by drikpanchang.com & ISRO)
# Computes tithi at sunrise using Lahiri ayanamsha (Government of India sidereal standard).
pyswisseph>=2.10.3

# Razorpay payment gateway (wire TODO blocks in payments.py once keys are set)
razorpay>=1.4.2

# Email notifications for vendor assignments
# Uses Python stdlib smtplib — no extra package needed for basic SMTP.
# For production-grade delivery, add sendgrid or AWS SES SDK here.
